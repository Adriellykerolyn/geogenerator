// 1. Eu seleciono os elementos que vou manipular no DOM
const campoArquivo = document.getElementById('arquivo');
const selectAgrupamento = document.getElementById('coluna-agrupadora');
const btnGerar = document.getElementById('gerar-relatorio');
const statusMsg = document.getElementById('status-processamento');
const downloadContainer = document.getElementById('download-link');
